const root = document.querySelector<HTMLDivElement>("#root");
if (root) {
  root.innerHTML = `
    <h1>Cairn</h1>
    <p>Phase 2 scaffold — side panel loads, no triage UI yet (Phase 3 M3).</p>
  `;
}

console.log("Cairn side panel loaded");
