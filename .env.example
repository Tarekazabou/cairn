# Copy to .env and fill in. Real values never committed — see CONTRIBUTING.md.
# Most of these are placeholders: the extraction service isn't built yet (Phase 3 M2),
# and the LLM provider hasn't been decided (see ADR-0001 open questions).

# services/extraction
EXTRACTION_SERVICE_PORT=8080
LLM_API_KEY=
LLM_PROVIDER=

# apps/extension (build-time, read by Vite)
VITE_EXTRACTION_SERVICE_URL=http://localhost:8080
